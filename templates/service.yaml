apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-tests.fullname" . }}
  labels:
    {{- include "k8s-tests.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8s-tests.selectorLabels" . | nindent 4 }}
